# Discord Configuration
DISCORD_TOKEN=your-discord-bot-token-here
DISCORD_GUILD_ID=your-discord-guild-id-here

# Database Configuration
# For production: point to your host's PostgreSQL instance
# For development: use this URL to connect to docker-compose postgres
DATABASE_URL=postgres://gambler:gambler_dev_password@localhost:5432/gambler_db?sslmode=disable

# Bot Configuration (optional - defaults shown)
INTEREST_RATE=0.005
INTEREST_SCHEDULE=0 0 * * *
STARTING_BALANCE=100000

# Development overrides (used in docker-compose.dev.yml)
LOG_LEVEL=debug